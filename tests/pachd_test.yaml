templates:
  - pachd-deployment.yaml
  - pachd-deployment.yaml
  - pachd-peer-deployment.yaml
tests:
  # or you can snapshot the whole manifest
  - it: manifest should match snapshot
    asserts:
      - matchSnapshot: {}